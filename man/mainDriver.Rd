% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Driver.R
\name{mainDriver}
\alias{mainDriver}
\title{Execute the complete analysis required to create version 2 or 3 of the WHO catalogue}
\usage{
mainDriver(
  correct_all = TRUE,
  LoF = TRUE,
  safe = TRUE,
  minMAF = MAF_THRESHOLD_REGULAR,
  lowMAFHet = TRUE,
  minQ = QUALITY_THRESHOLD_STRICT,
  lowQHet = TRUE,
  EXTRACTION_ID = EXTRACTION_ID,
  listIsolates = TRUE,
  OUTPUT_DIRECTORY = paste0("Results/", EXTRACTION_ID),
  DATA_DIRECTORY = "SOLO Algorithm Input files/DATABASE EXTRACTION files/",
  NON_DATABASE_DIRECTORY = system.file("extdata", package = "SOLOport")
)
}
\arguments{
\item{correct_all}{Logical; if TRUE, FDR correction uses all variants; if FALSE, only variants appearing as a SOLO in at least one isolate.}

\item{LoF}{Logical; if TRUE, pooled LoF mutations are included as hypotheses for FDR correction.}

\item{safe}{Logical; if TRUE, performs a full from-scratch conversion of variants across versions (slow).}

\item{minMAF}{Minimum minor allele frequency; variants below this threshold are treated as heterozygous.}

\item{lowMAFHet}{Logical; if TRUE, low-MAF variants become hets; if FALSE, they are filtered out.}

\item{minQ}{Minimum quality score; variants below this threshold are treated as heterozygous.}

\item{lowQHet}{Logical; if TRUE, low-quality variants become hets; if FALSE, they are filtered out.}

\item{EXTRACTION_ID}{Identifier string for the database extraction (used to locate input files); defaults to the package constant EXTRACTION_ID.}

\item{listIsolates}{Logical; if TRUE, records all isolates classified as solo at each iteration.}

\item{OUTPUT_DIRECTORY}{Directory for writing output files; defaults to Results/<EXTRACTION_ID>.}

\item{DATA_DIRECTORY}{Directory containing the database extraction files.}

\item{NON_DATABASE_DIRECTORY}{Directory containing non-database reference files (defaults to inst/extdata).}
}
\description{
Execute the complete analysis required to create version 2 or 3 of the WHO catalogue
}
