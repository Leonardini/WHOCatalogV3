% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DominantLineage.R
\name{computeDominantLineage}
\alias{computeDominantLineage}
\title{Compute the dominant sub-lineage for each sample from lineage marker data}
\usage{
computeDominantLineage(
  inputTab,
  mafThresholds = c(0.25, 0.75, 0.9),
  subLineage = TRUE,
  outDir = NULL
)
}
\arguments{
\item{inputTab}{A data frame with columns \code{sample_id}, \code{lineage_numbering}, and \code{final_af}.}

\item{mafThresholds}{A numeric vector of MAF thresholds (default \code{c(0.25, 0.75, 0.9)}).}

\item{subLineage}{Logical; if \code{TRUE} (default) extracts the sub-lineage (up to two dot-separated levels); if \code{FALSE} extracts only the broad lineage (one level).}

\item{outDir}{Optional directory for writing one output CSV per threshold; \code{NULL} skips file writing.}
}
\value{
A named list of data frames (names like \code{"maf0.25"}), each with columns \code{sample_id}, \code{dominant_lineage}, and \code{ties}.
}
\description{
For each MAF threshold, rows with \code{final_af >= threshold} are retained, the
\code{lineage_numbering} field is collapsed to the sub-lineage level (two
dot-separated segments with any leading \code{"lineage"} prefix stripped), and
the lineage supported by the most markers is chosen as the dominant one.  When
multiple lineages tie, the alphabetically first is reported as
\code{dominant_lineage} and the rest appear in \code{ties} (comma-separated).
Samples with no rows passing the threshold are still included with empty strings.
}
